export type GlowRingProps = {
    position: [number, number, number]
    innerRadius?: number
    outerRadius?: number
    color?: string
    opacity?: number
}

export type CityColumnProps = {
    name: string
    value: string
    height: number
    position: [number, number, number]
    color: string
}
