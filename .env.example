# 复制本文件为 .env 后才会生效。Vite 不会读取 .env.example。
# 改完 .env 必须重启 npm run dev。
#
# Key 类型必须是「Web端(JS API)」，并在控制台把 localhost 加入域名白名单。

# 高德 Web 端 JS API Key
VITE_AMAP_KEY=

# 高德安全密钥（securityJsCode）
VITE_AMAP_SECURITY_CODE=
